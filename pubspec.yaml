name: brain_forge_movies
description: "A Flutter project for the BrainForge recruitment task."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # Routing
  go_router: ^17.0.0
  uuid: ^4.5.2
  # Functional programming
  dartz: ^0.10.1
  equatable: ^2.0.5
  # State Management
  flutter_bloc: ^9.1.1
  shimmer_animation: ^2.1.0+1
  # Service locator
  get_it: ^9.1.1
  injectable: ^2.7.0
  flutter_native_splash: ^2.2.7
  # Networking
  dio: ^5.9.0
  connectivity_plus: ^7.0.0
  # Code Generation
  freezed: ^3.2.3  # Immutable models, union types, and pattern matching
  freezed_annotation: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  pretty_dio_logger: ^1.4.0
  json_serializable: ^6.3.1
  mockito: ^5.6.1
  # Code Generation Tools
  build_runner: ^2.4.6  # Code generation runner
  injectable_generator: ^2.9.1  # Injectable code generator

flutter:
  uses-material-design: true
